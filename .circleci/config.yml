version: 2.1
jobs:
  build:
    docker:
      - image: circleci/php:7.4-node-browsers
    steps:
      - checkout
      - run: sudo composer self-update
      - run: ls
      - run: composer install -n --prefer-dist --working-dir=./src/
      - run: npm ci --working-dir=./src/
      - run: npm run dev
      - run:
          name: php test
          command: vendor/bin/phpunit